{
  "name": "saks-signal",
  "version": "0.1.0",
  "description": "A library contianing code for Saks' Site Catalyst implementation",
  "main": "entry.js",
  "scripts": {
    "test": "eslint .",
    "release": "npm run test && webpack && git commit -am \"dist release\" && npm run version",
    "version": "npm version patch && git push && git push --tags"
  },
  "author": "Andrew Powell <powella@gilt.com>",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^3.18.0",
    "eslint-config-shellscape": "^1.0.0",
    "webpack": "^2.3.0"
  }
}
